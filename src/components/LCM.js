import React, { useState } from 'react';  function LCM() {   const [input1, setInput1] = useState('');   const [input2, setInput2] = useState('');   const [result, setResult] = useState('');    const gcd = (a, b) => {     while (b !== 0) {       [a, b] = [b, a % b];     }     return a;   };    const handleSubmit = () => {     const num1 = Math.abs(Number(input1));     const num2 = Math.abs(Number(input2));     if (isNaN(num1) || isNaN(num2) || num1 === 0 || num2 === 0) {       setResult('Vui lòng nhập số nguyên dương hợp lệ');       return;     }     const lcm = (num1 * num2) / gcd(num1, num2);     setResult(lcm);   };    const styles = {     container: { textAlign: 'center' },     input: { padding: '10px', width: '140px', fontSize: '16px', margin: '10px 5px' },     button: { padding: '10px 20px', fontSize: '16px', backgroundColor: '#007bff', color: '#fff', border: 'none', borderRadius: '5px', cursor: 'pointer 