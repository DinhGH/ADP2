import React, { useState } from 'react';  function CheckPalindrome() {   const [input, setInput] = useState('');   const [result, setResult] = useState('');    const handleSubmit = () => {     const str = input.trim();     if (!/^\d+$/.test(str)) {       setResult('Vui lòng nhập số hợp lệ');       return;     }     const reversed = str.split('').reverse().join('');     setResult(str === reversed ? 'Là số đối xứng' : 'Không phải số đối xứng');   };    const styles = {     container: { textAlign: 'center' },     input: { padding: '10px', width: '300px', fontSize: '16px', marginBottom: '10px' },     button: { padding: '10px 20px', fontSize: '16px', backgroundColor: '#007bff', color: '#fff', border: 'none', borderRadius: '5px', cursor: 'pointer' },     result: { marginTop: '15px', fontSize: '18px', color: '#333' },   };    return (     <div style={styles.container}>       <h2>Kiểm tra số đối xứng</h2>       <input         type="text"         placeholder="Nhập số"         value={input}         onChange={(e) => setInput(e.target.value)}         style={styles.input}       />       <br />       <button onClick={handleSubmit} style={styles.button}>         Kiểm tra       </button>       <div style={styles.result}>{result}</div>     </div>   ); }  export default CheckPalindrome;